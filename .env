# ============================================
# BROWSER CONFIGURATION
# ============================================
HEADLESS=False                    # True для CI/CD, False для локальной разработки
BROWSER=chromium                  # chromium, firefox, webkit
SLOW_MO=0                        # Замедление браузера в ms (для отладки: 1000)
TIMEOUT=30000                    # Глобальный timeout в ms
NAVIGATION_TIMEOUT=30000         # Timeout на загрузку страницы
VIEWPORT_WIDTH=1920
VIEWPORT_HEIGHT=1080

# ============================================
# EMULATION & RECORDING
# ============================================
USER_AGENT=                       # Если пусто - используется по умолчанию
LOCALE=en-US
TIMEZONE=UTC
TRACE=retain-on-failure           # off, on, retain-on-failure
SCREENSHOT=off                    # off, only-on-failure, on
VIDEO=off                         # off, retain-on-failure, on
DOWNLOADS_PATH=                   # Путь для скачивания файлов

# ============================================
# API CONFIGURATION
# ============================================
API_BASE_URL=https://api.example.com
API_TIMEOUT=30000
API_RETRY_ATTEMPTS=3
API_RETRY_DELAY=1000

# ============================================
# APPLICATION
# ============================================
BASE_DOMAIN=astanahub.com
ADMIN_URL=https://admin.example.com
ENV=dev                           # dev, qa, staging, prod
LOG_LEVEL=INFO                    # DEBUG, INFO, WARNING, ERROR

# ============================================
# TEST CREDENTIALS (только для локальной разработки!)
# ============================================
TEST_USER_EMAIL=a.baisaganov@astanahub.com
TEST_USER_PASSWORD=Pass1234!

# ============================================
# API TOKENS (чувствительная информация!)
# ============================================
API_TOKEN=
ADMIN_TOKEN=
